# lvl4-d5-py-modules

This app will request character information from the user. The user can give a name, choose a class, and assign stats to a character that is saved and read from a text file. 

To run, type below in the terminal:

py generator.py

The file it is reading and writing to is characters.txt